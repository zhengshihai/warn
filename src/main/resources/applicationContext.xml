<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx.xsd
        http://www.springframework.org/schema/aop
        http://www.springframework.org/schema/aop/spring-aop.xsd">


    <!-- 扫描warn包下所有使用注解的类型 -->
    <context:component-scan base-package="com.tianhai.warn"/>

    <!-- 启用AspectJ自动代理 -->
    <aop:aspectj-autoproxy proxy-target-class="true"/>

    <!-- 配置相关参数 -->
    <context:property-placeholder location="classpath:jdbc.properties,classpath*:mail.properties,classpath:application.properties" ignore-unresolvable="true"/>

    <beans profile="dev">
        <context:property-placeholder location="classpath:file-upload-dev.properties"/>
    </beans>

    <beans profile="prod">
        <context:property-placeholder location="classpath:file-upload-prod.properties"/>
    </beans>

</beans>